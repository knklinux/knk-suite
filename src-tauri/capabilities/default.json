{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos mínimos para las ventanas locales del workbench y su asistente.",
  "windows": ["main", "assistant"],
  "remote": {
    "urls": ["http://127.0.0.1:8086/*"]
  },
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
